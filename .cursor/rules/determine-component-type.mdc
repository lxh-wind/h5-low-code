---
description: 
globs: *.tsx
alwaysApply: false
---

🧠 Before generating a component, please decide:

## Will this component be a Server Component or a Client Component?

### Server Component ✅ (default):
- Use if:
  - You don’t need interactivity (no `useState`, `useEffect`, etc.)
  - The component uses server-only data fetching (`fetch()`, DB queries)
  - You want maximum performance and smaller JS bundle

### Client Component 🔄:
- Use if:
  - You need state, effects, or event handlers
  - You use browser-only APIs (`window`, `localStorage`, etc.)
  - You use third-party libraries that require the DOM

👉 Type one of the following:
- `server` – to create a Server Component (no `"use client"` directive)
- `client` – to create a Client Component (adds `"use client"` at the top)
